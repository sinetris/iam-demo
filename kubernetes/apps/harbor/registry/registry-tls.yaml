---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app: harbor
  name: harbor-registry-internal-tls
spec:
  secretName: harbor-registry-internal-tls
  dnsNames:
  - harbor-registry
  - harbor-registry.default
  - harbor-registry.default.svc
  - harbor-registry.default.svc.cluster
  - harbor-registry.default.svc.cluster.local
  issuerRef:
    kind: ClusterIssuer
    name: iam-demo-certs-issuer
