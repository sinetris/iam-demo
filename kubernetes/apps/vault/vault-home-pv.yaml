---
apiVersion: v1
kind: PersistentVolume
metadata:
  labels:
    app: vault
    type: local
  name: vault-home-pv
spec:
  accessModes:
  - ReadWriteMany
  capacity:
    storage: 500Gi
  hostPath:
    path: /var/local/data/vault-home
  storageClassName: local-path
