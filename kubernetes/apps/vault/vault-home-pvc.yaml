---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    app: vault
  name: vault-home-pvc
  namespace: tools
spec:
  accessModes:
  - ReadWriteMany
  resources:
    requests:
      storage: 200Gi
  storageClassName: local-path
  volumeMode: Filesystem
  volumeName: vault-home-pv
