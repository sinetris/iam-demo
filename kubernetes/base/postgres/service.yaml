---
apiVersion: v1
kind: Service
metadata:
  name: postgres
spec:
  type: ClusterIP
  ports:
    - name: pg-port
      port: 5432
      protocol: TCP
      targetPort: postgres-port
  selector:
    app.kubernetes.io/name: postgres
