---
apiVersion: v1
kind: Service
metadata:
  name: redis
  labels:
    app: redis
spec:
  type: ClusterIP
  ports:
    - name: redis-port
      port: 6379
      protocol: TCP
      targetPort: redis-port
  selector:
    role: data
